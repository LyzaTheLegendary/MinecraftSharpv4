namespace MinecraftSharp.Classes.Network
{
    public class MinecraftConnection
    {
        private readonly ConnectionTag m_tag;
        public MinecraftConnection(ConnectionTag tag)
        {
            m_tag = tag;
        }
    }
}
